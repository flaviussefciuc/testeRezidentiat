import {Component,computed,input} from '@angular/core';
@Component({selector:'app-trend',standalone:true,template:`<div class="trend-chart"><div class="chart-axis"><span>100%</span><span>75%</span><span>50%</span><span>25%</span><span>0%</span></div><div class="chart-area"><div class="chart-lines"><i></i><i></i><i></i><i></i><i></i></div>@if(points().length){<svg viewBox="0 0 600 180" preserveAspectRatio="none" role="img" aria-label="Evoluția procentului de puncte pe sesiuni"><defs><linearGradient id="fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#6b947b" stop-opacity=".25"/><stop offset="100%" stop-color="#6b947b" stop-opacity="0"/></linearGradient></defs><path [attr.d]="area()" fill="url(#fill)"/><polyline [attr.points]="line()" fill="none" stroke="#326652" stroke-width="3" vector-effect="non-scaling-stroke"/>@for(p of coords();track $index){<circle [attr.cx]="p.x" [attr.cy]="p.y" r="4" fill="#326652"><title>{{points()[$index].label}}: {{points()[$index].value}}%</title></circle>}</svg>}@else{<div class="chart-empty"><span class="empty-chart-icon">↗</span><strong>Progresul tău începe cu prima grilă.</strong><span>Rezolvă un test și vezi aici cum evoluezi.</span></div>}</div></div><div class="chart-labels">@for(p of points();track $index){@if($index===0||$index===points().length-1){<span>{{p.label}}</span>}}@empty{<span>Prima sesiune</span><span>Următorul pas</span>}</div>`,})
export class ChartComponent {points=input<{value:number,label:string}[]>([]);coords=computed(()=>this.points().map((p,i)=>({x:this.points().length===1?300:12+i*576/(this.points().length-1),y:174-p.value*1.68})));line=computed(()=>this.coords().map(p=>`${p.x},${p.y}`).join(' '));area=computed(()=>this.coords().length?`M ${this.coords()[0].x} 180 L ${this.coords().map(p=>`${p.x} ${p.y}`).join(' L ')} L ${this.coords().at(-1)!.x} 180 Z`:'');}

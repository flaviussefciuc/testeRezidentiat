import {Component,computed,inject,signal} from '@angular/core';
import {FormsModule} from '@angular/forms';
import {RouterLink} from '@angular/router';
import {Store} from './store';
import {QUESTIONS} from './questions';
import {TOPICS} from './topics';
import {summarize,topicStats,improvement} from './scoring';
import {ChartComponent} from './chart.component';
import {IconComponent} from './icon.component';
@Component({standalone:true,imports:[FormsModule,RouterLink,ChartComponent,IconComponent],template:`<div class="page-heading"><div><div class="eyebrow">VEZI CÂT DE DEPARTE AI AJUNS</div><h1>Progresul tău, pe bune.</h1><p>Înțelege ce stăpânești și unde merită să revii.</p></div><button class="button secondary" (click)="store.export()" [disabled]="!store.attempts().length"><app-icon name="download"/>Exportă rezultatele</button></div>
<div class="filter-bar"><label>Perioadă<select [ngModel]="period()" (ngModelChange)="period.set(+$event)"><option [ngValue]="0">De la început</option><option [ngValue]="7">Ultimele 7 zile</option><option [ngValue]="30">Ultimele 30 de zile</option><option [ngValue]="90">Ultimele 90 de zile</option></select></label><label>Tip de sesiune<select [ngModel]="mode()" (ngModelChange)="mode.set($event)"><option value="all">Toate sesiunile</option><option value="practice">Antrenamente</option><option value="exam">Simulări</option></select></label>@if(store.user()){<button class="text-link" (click)="store.sync()"><app-icon name="refresh"/>Sincronizează</button><span class="small muted">{{store.syncState()}}</span>}@else{<span class="small muted">Vizitator · rezultate salvate în acest browser</span>}</div>
<div class="stats-grid three"><article class="stat"><span class="stat-icon mint"><app-icon name="target"/></span><div><span>Punctaj cumulat</span><strong>{{summary().max?summary().percent+'%':'—'}}</strong><small>{{summary().points}} / {{summary().max}} puncte</small></div></article><article class="stat"><span class="stat-icon peach"><app-icon name="check"/></span><div><span>Complet corecte</span><strong>{{summary().correct}}<small> / {{summary().total}}</small></strong><small>Toate variantele identificate corect</small></div></article><article class="stat"><span class="stat-icon lavender"><app-icon name="chart"/></span><div><span>Evoluție în 7 zile</span><strong>{{delta()===null?'—':(delta()!>0?'+':'')+delta()+' pp'}}</strong><small>{{delta()===null?'Sunt necesare sesiuni în ambele săptămâni':'Față de cele 7 zile precedente'}}</small></div></article></div><section class="panel"><div class="section-heading"><div><h3>Fiecare sesiune, un reper nou</h3><p>Ultimele 20 de sesiuni din perioada și tipul selectat</p></div><span class="badge">{{attempts().length}} sesiuni</span></div><app-trend [points]="points()"/></section>
<div class="section-heading lower-heading"><div><h3>Unde ai nevoie de mai mult exercițiu?</h3><p>Capitolele exersate sunt ordonate de la cel mai mic punctaj. Un eșantion mic nu indică încă nivelul de pregătire.</p></div></div><section class="panel table-panel"><div class="table-scroll"><table><thead><tr><th>Capitol</th><th>Grile evaluate</th><th>Punctaj obținut</th><th>Următorul pas</th></tr></thead><tbody>@for(t of stats();track t.id){<tr><td><strong>{{t.name}}</strong><small>{{t.book}}</small></td><td>{{t.total}} @if(t.total>0&&t.total<10){<small>Eșantion mic</small>}</td><td><div class="table-progress"><div class="progress-track"><span [style.width.%]="t.percent" [class.low]="t.percent<60"></span></div><strong>{{t.total?t.percent+'%':'Neexersat'}}</strong></div></td><td><a class="text-link" routerLink="/teste" [queryParams]="{topic:t.id}">Exersează →</a></td></tr>}</tbody></table></div></section>
<div class="section-heading lower-heading"><h3>Istoricul sesiunilor</h3></div><section class="panel table-panel"><div class="table-scroll"><table><thead><tr><th>Data</th><th>Sesiune</th><th>Grile</th><th>Punctaj</th><th>Revizuire</th></tr></thead><tbody>@for(a of attempts();track a.id){<tr><td>{{date(a.completedAt)}}</td><td>{{a.mode==='exam'?'Simulare':'Antrenament'}}</td><td>{{a.answers.length}}</td><td><strong>{{summarize(a.answers,questions).points}} / {{summarize(a.answers,questions).max}}</strong></td><td><a [routerLink]="['/rezultat',a.id]" class="text-link">Vezi răspunsurile →</a></td></tr>}@empty{<tr><td colspan="5" class="empty-inline">Nicio sesiune în această perioadă. <a routerLink="/teste">Începe un test.</a></td></tr>}</tbody></table></div></section><p class="small muted">Punctajul cumulat este ponderat cu punctajul maxim al grilelor. „pp” înseamnă puncte procentuale. Evoluția în 7 zile compară două săptămâni consecutive, pentru tipul de sesiune ales, independent de filtrul perioadei.</p>`})
export class ProgressComponent {store=inject(Store);period=signal(0);mode=signal('all');questions=QUESTIONS;summarize=summarize;attempts=computed(()=>this.store.attempts().filter(a=>(!this.period()||Date.parse(a.completedAt)>Date.now()-this.period()*86400000)&&(this.mode()==='all'||a.mode===this.mode())).sort((a,b)=>b.completedAt.localeCompare(a.completedAt)));summary=computed(()=>summarize(this.attempts().flatMap(a=>a.answers),QUESTIONS));delta=computed(()=>improvement(this.store.attempts().filter(a=>this.mode()==='all'||a.mode===this.mode()),QUESTIONS));points=computed(()=>[...this.attempts()].reverse().slice(-20).map(a=>({value:summarize(a.answers,QUESTIONS).percent,label:new Date(a.completedAt).toLocaleDateString('ro-RO',{day:'numeric',month:'short'})})));stats=computed(()=>TOPICS.map(t=>({...t,...topicStats(this.attempts(),QUESTIONS,t.id)})).sort((a,b)=>(a.total?a.percent:101)-(b.total?b.percent:101)));date(d:string){return new Date(d).toLocaleString('ro-RO',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'});}}
